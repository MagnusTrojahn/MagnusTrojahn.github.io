{
  "devDependencies": {
    "gh-pages": "^6.3.0"
  },
  "homepage": "https://magnustrojahn.github.io",
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "predeploy": "npm run build",
    "deploy": "gh-pages -b master -d build"
  }
}
